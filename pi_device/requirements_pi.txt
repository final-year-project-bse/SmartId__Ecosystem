# Raspberry Pi dependencies for SmartID device scripts
# Install with:  pip install -r requirements_pi.txt

# HTTP client (server API calls)
requests>=2.28

# RFID reader (MFRC522 via SPI)
mfrc522>=0.0.7

# Face recognition (requires dlib — takes ~30 min to compile on Pi)
# Pre-built wheels available: https://github.com/ageitgey/face_recognition
face_recognition>=1.3
opencv-python>=4.6

# Fingerprint sensor AS608/R307 (via UART)
pyfingerprint>=2.1

# GPIO (pre-installed on Raspberry Pi OS; included here for clarity)
RPi.GPIO>=0.7
